/ %h1 Signup for Grab N Grub's new list service.
/ %h2 Never forget your family's needs again!

/ = form_for :customer, url: '/customers' do |f|
/   = f.label, :name
/   = f.text_field, :name
/   = f.label, :email
/   = f.email_field, :email
/   = f.label, :password
/   = f.password_field, :password
/   = f.label, :password_confirmation
/   = f.password_field, :password_confirmation

/   = f.submit 'Signup'
